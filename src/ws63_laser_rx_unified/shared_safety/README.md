# Shared Safety

R1 keeps this directory as the future home for route-independent safety guards.

The first rule remains simple: route startup, route switching, abort, disconnect,
or fatal route error must leave the laser physically off.

No shared safety code is compiled from this directory yet.
